# ============================================================
# CONFIGURACION GENERAL (no sensible)
# Estos valores pueden variar por entorno pero no son secretos
# ============================================================

APP_NAME=Maria Test
PORT=3000
NODE_ENV=development
LOG_LEVEL=primer
FEATURE_X_ENABLED=false

# ============================================================
# SECRETOS
# NUNCA subas valores reales al repositorio
# En producción estos valores se inyectan desde el sistema
# de gestión de secretos (Vault, AWS Secrets Manager, etc.)
# ============================================================

DB_PASSWORD=cambia_esto_por_tu_password_real
API_KEY=cambia_esto_por_tu_api_key_real
JWT_SECRET=cambia_esto_por_un_secreto_de_al_menos_32_caracteres_!!
